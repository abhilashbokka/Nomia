# docs

`logo.png` goes here (provided separately from Claude Design) — referenced by the main
[README](../README.md) once added.

`screenshots/` holds real screenshots of the running app (captured via a Playwright pass against
the actual local server, not mockups), used in the main README's UI section.
